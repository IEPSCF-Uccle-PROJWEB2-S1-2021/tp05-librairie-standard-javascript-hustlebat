result


function analyzeTemperature(quantity, data) {
  if (quantity === 0 ) 
  console.log(quantity);
  
  for (let i = 0; i < quantity.length; i++) {
    const data = array[1];
  }

  if (Math.abs(result)) > Math.abs(data) {
    if (data > 0) { 
      result -data
      
    }
  }
 else {   
  
  return 0;}
}
